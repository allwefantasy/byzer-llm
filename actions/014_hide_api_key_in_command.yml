source_dir: /Users/allwefantasy/projects/byzer-llm
target_file: /Users/allwefantasy/projects/byzer-llm/output.txt

model: gpt3_5_chat
enable_multi_round_generate: false
index_model: haiku_chat

skip_build_index: false
index_filter_level: 0

execute: true
auto_merge: true
human_as_model: true

query: >
  在 byzerllm_command.py 文件中，我们会通过如下代码打印所有参数：

  ```python
  print("Command Line Arguments:")
    print("-" * 50)
    for arg, value in vars(args).items():        
        print(f"{arg:20}: {value}")
    print("-" * 50)    
  ```

  当打印 infer_params 参数infer_params， 该值是一个 dict, 如果发现里面有 saas.api_key 参数，那么将其
  value 设置为 *****




